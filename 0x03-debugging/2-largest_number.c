#include "main.h"

/**
 * largest_number - entry point
 *@a: first integer
 *@b: second integer
 *@c: third integer
 * Return: the larger number.
 */

int largest_number(int a, int b, int c)
{
	int largerst = b;

	if (a > b)
	{
		largerst = a;
	}
	else if
	{
		largest = c;
	}
	return (largerst);
}
